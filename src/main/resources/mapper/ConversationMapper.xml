<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.icbc.digitalhuman.mapper.ConversationMapper">

    <insert id="create">
        INSERT INTO conversation (id, evaluation, feedback, update_time, username)
        VALUES (#{id}, #{evaluation}, #{feedback}, #{updateTime}, #{username})
    </insert>

</mapper>